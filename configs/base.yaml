project:
  name: AROMA
  seed: 42

models:
  primary:
    id: meta-llama/Llama-3.2-11B-Vision-Instruct

  generalization:
    id: Qwen/Qwen2.5-VL-7B-Instruct

generation:
  do_sample: false
  temperature: 0.0
  max_new_tokens_direct: 32
  max_new_tokens_ledger: 512

ledger:
  duplicate_iou_threshold: 0.5

diagnostics:
  matching_iou_threshold: 0.5

experiment:
  pilot_seed: 42
