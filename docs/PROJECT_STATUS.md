
# AROMA Project Status

## Scientific milestone

The current single-model AROMA experimental line is **closed and frozen**.

Primary model:

```text
meta-llama/Llama-3.2-11B-Vision-Instruct
```

Revision:

```text
9eb2daaa8597bf192a8b0e73f848f3a102794df5
```

Primary actuator:

```text
Layer 18
Head 13
head dimension = 128
hidden slice = [1664:1792]
```

## Closed experimental lines

The following are treated as completed scientific records:

```text
L18H13 causal mechanism discovery

bidirectional directional characterization

v2 adaptive routing development

frozen v3 procedural confirmation

frozen zero-shot TallyQA transfer

TallyQA transfer forensics

SPUC support-projection diagnostic

TallyQA natural-domain utility learning

K=500 frozen TallyQA confirmation

HoloCount external boundary study

response-derived routing Gate 0 / CRR

CSA Gate A

naive CSA Gate B1

strength audit

MN-CSA Gate B2

Bottom-4 specificity Gate B3a

random rank-4 specificity Gate B3b

untouched v4 final confirmation
```

These results should not be reopened solely to improve headline numbers.

## Frozen negative results

The following negative or mixed results are part of the scientific record:

```text
TallyQA zero-shot routing:
-2.975 pp

HoloCount transferred routing:
-0.0403 pp

CSA Gate-A promotion criterion:
NO-GO

naive same-alpha CSA:
clear negative

response-derived routing extension:
NO-GO / weak signal
```

They must not be retrospectively reframed as successes.

## Final v4 result

```text
Baseline:
50.25%

Whole-head:
57.25%

MN-CSA:
58.25%

MN-CSA vs baseline:
+8.00 pp

MN-CSA vs whole-head:
+1.00 pp

exact McNemar p:
0.0077877
```

## Current supported interpretation

AROMA separates:

```text
Actuator Capacity
        ↓
Actuation Geometry
        ↓
Routing Policy
```

Two principal findings are:

```text
Sensitivity is not activation energy.

An actuator is not a policy.
```

## Main limitations

The current study remains limited by:

```text
single base VLM architecture

procedural rather than natural final MN-CSA confirmation

failed zero-shot natural routing

failed HoloCount routing transfer

failure of the existing 39-feature linear utility family
to recover HoloCount oracle capacity
```

## Recommended next research phase

The highest-value next experiment is an architecture-first
**cross-model mechanistic replication**.

It should be conducted under a new frozen protocol and should not modify the
completed Llama-3.2-Vision experiments.

A second priority, if resources permit, is frozen MN-CSA validation on a
natural-image domain.

## Explicit non-goals

Do not reopen the closed line merely to:

```text
tune rank on v4
tune alpha on v4
change MN normalization after v4
increase random-control count only to reduce p-value
rescue HoloCount routing
rescue CRR
lower Gate-A thresholds
generate repeated procedural confirmation datasets
```
